// =============================================================================
// STYLES - TaxiDash Madrid
// =============================================================================
import { StyleSheet, Platform, Dimensions } from 'react-native';

const { width: SCREEN_WIDTH } = Dimensions.get('window');

export HEADER

# Append the styles body
cat /tmp/styles_body.ts >> /app/frontend/app/styles/index.ts

# Check the result
head -20 /app/frontend/app/styles/index.ts
echo "..."
tail -10 /app/frontend/app/styles/index.ts
echo ""
wc -l /app/frontend/app/styles/index.ts
